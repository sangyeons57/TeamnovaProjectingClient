<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeamnovaPersonalProjectProjecting"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.FindPasswordActivity" />
        <activity android:name=".activity.JoinActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.PersonalSettingActivity" />
        <activity android:name=".ui.profile.setting.ChangePasswordActivity" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".activity.SearchActivity" />
        <activity android:name=".activity.FriendsActivity" />
        <activity android:name=".activity.FriendsAcceptActivity" />
        <activity android:name=".activity.project.AddProjectActivity" />
        <activity android:name=".activity.project.AddProjectSetNameActivity" />
        <activity android:name=".activity.project.JoinNewProject" />
        <activity android:name=".activity.project.AddCategoryActivity" />
        <activity android:name=".activity.project.AddChannelActivity" />
        <activity android:name=".activity.project.EditCategoryActivity" />
        <activity android:name=".activity.project.EditChannelActivity" />
        <activity android:name=".activity.project.ProjectSettingActivity" />
        <activity android:name=".activity.EditProfileActivity" />
    </application>

</manifest>